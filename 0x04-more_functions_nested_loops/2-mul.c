#include "main.h"
/**
 * mul -  checks for checks for a digit (0 through 9).
 * @a: a -  Variable
 * @b: b - variable
 * Return: Always 0.
 */
char mul(char a, char b)
{
	char mul;

	_putchar("mul = a * b");

	return (mul);
}
